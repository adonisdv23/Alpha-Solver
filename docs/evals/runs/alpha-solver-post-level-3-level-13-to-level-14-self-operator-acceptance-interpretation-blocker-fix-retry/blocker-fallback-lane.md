# Blocker fallback lane

If this lane's outputs are later found defective (for example, the group was
misclassified, a machine-readable `ArtifactStoreError` record is found that
this review missed, or this packet misstates the verification results), the
fallback lane is:

```
ALPHA-SOLVER-POST-LEVEL-3-LEVEL-13-TO-LEVEL-14-SELF-OPERATOR-ACCEPTANCE-INTERPRETATION-BLOCKER-FIX-RETRY-FIX-001
```
