# Non-Actions

This docs-only packet did not:

- score outputs;
- change scores;
- unblind outputs;
- reveal source identities;
- infer or reconstruct source identities;
- access an identity map;
- create final interpretation;
- call providers;
- run local models;
- expose runtime endpoints;
- expose `/v1/solve`;
- expose dashboard or public API behavior;
- mutate Google Sheets or backlog workbooks;
- add dependencies;
- edit runtime, provider, model, routing, SAFE-OUT, budget, determinism, observability, replay, or SolverEnvelope behavior.
