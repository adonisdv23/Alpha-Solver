# Approved target

The operator-approved first supervised-use target, exactly as supplied in
the operator approval fields for this lane:

```text
existing evidence packet consistency review of the Self Operator evidence
chain, as defined in use-target.md of the first-supervised-use packet.
```

Defining document (read-only input):
`docs/evals/runs/alpha-solver-post-level-3-level-14-self-operator-first-supervised-use-packet/use-target.md`

The target consists of (per that document): one docs-only proposed task
processed through the Self Operator gate-and-record pipeline (the local
dry-run wrapper classifies the proposed command text; it executes nothing),
followed by a supervised run of the deterministic read-only packet
consistency checker over the existing Self Operator evidence chain, with
all outputs preserved below the output root. Nothing beyond this target was
approved or executed.
