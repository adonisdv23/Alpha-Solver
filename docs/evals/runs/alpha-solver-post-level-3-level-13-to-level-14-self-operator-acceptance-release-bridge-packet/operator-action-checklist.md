# Operator action checklist

Before using this bridge packet for later lanes, the operator must:

- recheck live GitHub for merged PRs and active branches;
- confirm PR #449 remains merged and GS done;
- confirm Level 10 through Level 12 prerequisite implementation work is merged and GS done;
- confirm the active lane is not conflicting with another Codex task;
- confirm explicit operator confirmation text is present before any run;
- confirm the run is local-only and operator-supervised;
- confirm no provider, hosted model, external API, deployment, billing, browser automation, credential, Google Sheets, source-artifact mutation, or evidence-promotion action is planned;
- stop if explicit operator confirmation is missing.
