# Current-state assumptions

- PR #449 hard-stop fix is merged and GS done.
- Static-test scaffold implementation is the current active implementation lane or is expected to be completed before acceptance.
- Acceptance cannot start until static safety, artifact schema, preflight, approval, stop-state, and dry-run harness work is merged and GS done.
- Live GitHub must be rechecked before acting because branch state, merged PRs, and active lanes can change after this bridge packet is created.
