# Deferred items

| item_id | priority | reason | revisit trigger |
|---|---|---|---|
| `AUDIT-008` | C | Final local status CLI remains optional operator UX and is outside this lane. | A future operator-approved status CLI implementation spec exists. |
| `AUDIT-009` | C | The missing-prerequisite test requirement applies only if the deferred final local status CLI is later authorized. | `AUDIT-008` is activated by a future implementation spec. |
