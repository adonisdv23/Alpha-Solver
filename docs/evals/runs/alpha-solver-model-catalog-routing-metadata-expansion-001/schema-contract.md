# Schema contract

Each model entry carries identity, display name, provider, backend type, model family, availability status, provider-key requirement, local-only flag, smoke eligibility, cost tier, latency tier, context tier, privacy tier, capability tags, route tags, tool compatibility, best-for notes, avoid-for notes, fallback eligibility, operator notes, and review timestamps. Conservative values are used where metadata is catalog-only.
