# Operator-facing boundary

This lane is product-foundation catalog work only. It does not run Alpha runtime behavior, invoke `/v1/solve`, call providers, run hosted or local models, call Ollama, browse, execute tools, mutate Sheets, unblind, inspect A/B keys, inspect source maps, score, or interpret results.
