# Non-actions

This packet does not implement Self Operator, modify runtime, create tests, run tests, call providers, expose `/v1/solve`, expose dashboards, configure credentials, run models, run benchmarks, deploy, perform billing work, autonomously merge, control browsers, or promote evidence.

It also does not modify provider code, API code, dashboard code, CLI behavior, checker scripts, Makefile, CI, source-artifact files, or non-target docs.
