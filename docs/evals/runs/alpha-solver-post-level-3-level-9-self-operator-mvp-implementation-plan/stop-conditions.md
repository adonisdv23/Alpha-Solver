# Stop conditions

Stop immediately if any future implementation would:

- start Level 10 before this packet is accepted and merged;
- exceed static test scaffold scope;
- modify runtime, provider, API, dashboard, CLI, tests beyond the authorized static scaffold, checker scripts, Makefile, CI, source-artifact files, or non-target docs;
- make provider calls, hosted model calls, or external API calls;
- use credentials, browser automation, deployment, billing, route exposure, fallback, or evidence promotion;
- omit staged or unstaged diff proof;
- omit raw artifacts or reviewer notes;
- conflict with any Level 8 boundary.
