# DEF-003 Path C replacement audit boundary

**SPEC ONLY, NOT EXECUTED HERE.**

A replacement audit is independent evidence only. It does not reconstruct, confirm, recover, validate, or stand in for the missing original.

A replacement audit requires explicit operator authorization. It requires token/USD/run-count caps before any provider call. It must live in a separate future packet. It must label the evidence track: `replacement evidence`.

It must not be run in this PR.

## Blank authorization and execution fields

* Authorization:
* Max tokens:
* Max USD:
* Run count:
* Model/provider:
* Inputs:
* Prompt-contract:
* Pass/interpretion criteria:
* Output packet id:
