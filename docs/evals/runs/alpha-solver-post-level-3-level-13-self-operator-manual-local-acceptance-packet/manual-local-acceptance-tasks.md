# Manual Local Acceptance Tasks

Statuses are placeholders only. Do not fabricate results.

| Task ID | Objective | Prerequisite | Input placeholder | Expected artifacts | Expected result | Stop condition | Raw output placeholder | Defect placeholder | Interpretation placeholder |
|---|---|---|---|---|---|---|---|---|---|
| MLA-001 | Safe approved local dry-run wrapper path. | Approval true, explicit confirmation, safe local output root. | Proposed safe local-only task and matching approval record. | `dry-run-result.json`, `execution-gate-result.json`. | `ready_for_operator_supervised_local_dry_run`; does not execute proposed commands. | Stop if artifact path escapes output root or command execution occurs. | NOT RUN | NOT RUN | NOT RUN |
| MLA-002 | Missing approval blocks. | Safe local output root and proposed task. | Proposed task with no approval record. | `dry-run-result.json`, `execution-gate-result.json`, `stop-state.json` when blocked. | Blocked by missing approval. | Stop if readiness is reported without approval. | NOT RUN | NOT RUN | NOT RUN |
| MLA-003 | False approval blocks. | Safe local output root and proposed task. | Proposed task with approval explicitly false. | `dry-run-result.json`, `execution-gate-result.json`, `stop-state.json` when blocked. | Blocked by false approval. | Stop if readiness is reported with false approval. | NOT RUN | NOT RUN | NOT RUN |
| MLA-004 | Approval identity mismatch blocks. | Approval identity mismatch fixture/inputs. | Proposed task and approval record with mismatched identity. | `dry-run-result.json`, `execution-gate-result.json`, `stop-state.json` when blocked. | `blocked_by_approval_identity_mismatch` / `approval_identity_mismatch` using `SELF_OPERATOR_APPROVAL_IDENTITY_MISMATCH`. | Stop if mismatch is not blocked. | NOT RUN | NOT RUN | NOT RUN |
| MLA-005 | Unsafe command blocks. | Unsafe command classification input. | Proposed task containing unsafe command. | `dry-run-result.json`, `execution-gate-result.json`, `stop-state.json` when blocked. | Unsafe command is blocked. | Stop if unsafe command becomes ready. | NOT RUN | NOT RUN | NOT RUN |
| MLA-006 | Path traversal blocks. | Temporary output root. | Artifact path containing traversal attempt. | `dry-run-result.json` or path-safety failure artifact if produced. | Path traversal is blocked. | Stop if output can escape root. | NOT RUN | NOT RUN | NOT RUN |
| MLA-007 | Overwrite attempt blocks. | Existing placeholder output file under temporary root. | Output path that would overwrite existing artifact. | Path-safety or wrapper result artifact. | Overwrite attempt is blocked. | Stop if existing artifact is overwritten. | NOT RUN | NOT RUN | NOT RUN |
| MLA-008 | Redaction preservation. | Input with redactable synthetic sensitive value. | Proposed task/metadata containing synthetic secret-like value. | Redacted `dry-run-result.json` and related artifacts. | Redaction status is safe and raw sensitive value is absent. | Stop if redaction fails. | NOT RUN | NOT RUN | NOT RUN |
| MLA-009 | Evidence boundary preservation. | Evidence boundary text reviewed. | Proposed task within local-only acceptance boundary. | Result artifact with evidence boundary. | Evidence boundary preserved; does not run acceptance. | Stop on provider/API/browser/deploy/billing/credential/Google Sheets/source-artifact/evidence-promotion action. | NOT RUN | NOT RUN | NOT RUN |
| MLA-010 | Non-execution proof. | Operator understands dry-run-only behavior. | Any allowed dry-run wrapper input. | Result artifact with non-execution confirmation. | Artifact confirms proposed commands were not executed. | Stop if proposed commands execute. | NOT RUN | NOT RUN | NOT RUN |
