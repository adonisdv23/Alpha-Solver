# Operator Stop Conditions

Hard stop:

`stop if explicit operator confirmation is missing`

Additional stops:

- Stop if output root is not temporary/local.
- Stop if source files would be modified.
- Stop if provider/API/browser/deploy/billing/credential/Google Sheets access is required.
- Stop if acceptance results are missing.
- Stop if readiness conclusion is being requested before interpretation.
