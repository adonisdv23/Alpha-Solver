# Operator UI contract

- Preview remains above and separate from smoke execution.
- Preview labels say `Preview route only`.
- Smoke labels say `Run bounded smoke check`.
- No API key field is introduced.
- No persistence, external assets, public deployment, or new runtime calls are introduced.
