# Manual override contract

Supported controls remain limited to existing console parameters:

- Mode override through the existing mode dropdown.
- Model override through the existing model dropdown or custom model input.

Unavailable states are explicit:

- Route override is unavailable in this lane.
- Tool override is unavailable in this lane.
