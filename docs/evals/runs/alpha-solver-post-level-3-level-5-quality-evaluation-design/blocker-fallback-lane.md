# Blocker Fallback Lane

If this packet is incomplete, inconsistent, unsafe, stale, overbroad, contradictory, non-reproducible, or unable to preserve the accepted evidence boundary, use blocker fallback lane:

`ALPHA-SOLVER-POST-LEVEL-3-LEVEL-5-QUALITY-EVALUATION-DESIGN-FIX-001`

The fallback lane is for documentation repair only unless a later explicit authorization changes that scope. It must not run benchmarks, run local model inference, score outputs, expose routes, call providers, modify runtime behavior, or promote evidence.
