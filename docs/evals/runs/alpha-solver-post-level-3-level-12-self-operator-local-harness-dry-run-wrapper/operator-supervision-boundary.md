# Operator supervision boundary

Human operator confirmation remains required. The dry-run wrapper only prepares an operator-supervised local handoff and records readiness for a later manual local acceptance packet. It does not approve autonomously, execute tasks autonomously, run acceptance, merge, release, or claim MVP readiness.
