# Approval, artifact, and stop-state gates

The future static scaffold must require explicit operator approval gates, complete local artifact schemas, and fail-closed stop-state outputs.

Expected finding IDs:

- `SELF_OPERATOR_APPROVAL_GATE_REQUIRED`
- `SELF_OPERATOR_ARTIFACT_SCHEMA_INCOMPLETE`
- `SELF_OPERATOR_STOP_STATE_REQUIRED`
