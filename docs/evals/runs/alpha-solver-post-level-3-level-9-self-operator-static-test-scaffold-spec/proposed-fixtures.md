# Proposed fixtures

Proposed inert fixture paths for a future code lane, not created by this packet:

- `tests/fixtures/self_operator/inert_operator_request.json`
- `tests/fixtures/self_operator/inert_approval_record.json`
- `tests/fixtures/self_operator/inert_artifact_manifest.json`
- `tests/fixtures/self_operator/inert_preflight_result.json`

Fixtures must be synthetic and must not contain secrets, provider output, external API output, billing data, browser data, deployment output, or evidence-promotion claims.
