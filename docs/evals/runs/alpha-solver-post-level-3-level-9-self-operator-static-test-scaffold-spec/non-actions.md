# Non-actions

Docs-only static test scaffold specification. This does not implement or run tests, create fixtures, modify runtime, call providers, call external APIs, access credentials, automate browsers, deploy, bill, expose routes, add fallback, or promote evidence.
