# Evidence boundary

This lane implements local-only result import tooling and writes a deterministic import summary.

It does not interpret evidence, update Google Sheets, call providers, run hosted or local models, automate browsers, deploy, bill, touch credentials, mutate source artifacts, promote evidence, or claim MVP readiness.
