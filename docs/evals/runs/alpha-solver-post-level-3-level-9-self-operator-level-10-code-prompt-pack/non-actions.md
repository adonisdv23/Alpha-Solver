# Non-actions

Docs-only Codex prompt pack. This does not run Codex prompts, implement code, authorize implementation by itself, modify runtime, call providers, call external APIs, expose routes, deploy, perform billing work, automate browsers, add fallback, mutate source artifacts, or promote evidence.
