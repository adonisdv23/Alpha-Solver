# Failed-Closed Non-Exposure Preservation

Failed-closed boundary and safety paths keep normal answer fields empty. The diagnostic reset adds enum-only metadata and does not expose raw Pass 1 or Pass 2 text through `gate_trace`.

Prompt 5-style boundary/echo attempts remain blocked or failed closed with `answer`, `final_answer`, `considerations`, and `assumptions` empty.
