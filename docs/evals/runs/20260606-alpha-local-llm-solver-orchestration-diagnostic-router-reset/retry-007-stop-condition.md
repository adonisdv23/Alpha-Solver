# Retry 007 Stop Condition

If retry 007 still fails Prompt 2 or Prompt 3 after diagnostic-router reset, the next lane must not be another blind behavior patch. It must use diagnostic reason codes to classify the failure as one of:

- implementation bug;
- deterministic scanner false positive;
- Pass 2 model output limitation;
- qwen2.5:3b local-model behavior limitation;
- prompt expectation mismatch requiring spec review.
