# Required preflight checks

Allowed future checks may include:

- local repo status;
- branch name;
- changed-file scope;
- required docs existence;
- checker availability;
- Python availability;
- local command dry-run validation;
- staged and unstaged diff checks.

Checks must not make network calls or execute providers, models, browsers, deployment tools, billing tools, or route exposure.
