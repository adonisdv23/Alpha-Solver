# Output artifacts

Future output artifacts may include:

- preflight summary JSON;
- command transcript;
- changed-file scope proof;
- stop-state JSON;
- reviewer notes.

Artifacts must be local-only and must exclude secrets, credentials, provider outputs, external API responses, browser data, deployment output, billing data, and evidence-promotion labels.
