# Non-actions

Docs-only preflight runner spec. This does not create a runner, execute tasks, install packages, fetch remotes, call providers, call external APIs, access credentials, control browsers, deploy, bill, expose routes, or promote evidence.
