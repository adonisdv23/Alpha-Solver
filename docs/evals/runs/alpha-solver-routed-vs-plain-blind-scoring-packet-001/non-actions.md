# Non-actions

This lane did not run Alpha runtime, invoke `/v1/solve`, call providers, run hosted models, run local models, execute tools, browse, use current external research, mutate Google Sheets, add dependencies, expose dashboard/public API behavior, deploy, score outputs, fill scoring fields, lock scores, compare results, unblind, inspect an unblinding map, commit an identity map, or interpret value/quality/readiness.
