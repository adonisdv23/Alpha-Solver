# Blocker Fallback Lane

## Fallback lane

`ALPHA-LOCAL-LLM-SOLVER-ORCHESTRATION-LEVEL-3-VALIDATION-DESIGN-PACKET-FIX-001`

## When to use

Use this lane if the design packet is incomplete, unsafe, internally inconsistent, blocked, missing provenance, or found to have crossed any execution, fallback, hosted provider, dashboard, `/v1/solve`, benchmark, billing, or evidence-promotion boundary.

## Fallback boundary

The fallback lane must remain docs-only unless a later explicit lane says otherwise. It must not execute validation or promote evidence.
