# RVP-006 manual output record

| Field | Value |
|---|---|
| Task ID | `RVP-006` |
| Task family | coding task |
| Original task prompt | Design a small pure function and focused tests for parsing a CSV row with missing optional fields. |
| Timestamp / run label | 2026-06-17 manual no-provider prompt-contract simulation |
| Collector/source label | manual no-provider prompt-contract simulation |
| Task ID preserved | yes |
| Sensitive/private data included beyond authorized evaluation frame | no |
| Collection status | collected |
| Safe for later blinded scoring | yes, after scorer-facing blinding removes route/source labels as required by a future scoring protocol |

## Plain baseline output

Define a pure function that accepts a CSV row string and a schema of required and optional fields, returns a structured record, and represents missing optional values as None or an agreed default. Focus tests on full rows, missing optional trailing fields, empty optional fields, missing required fields, extra columns, quoted commas, and whitespace normalization.

## Routed Alpha output

- Task family: coding task
- Selected route family: coding-with-tests route
- Model recommendation: Code-capable model
- Tool recommendation: local tests recommended in later authorized implementation; no files edited
- Route reasons: The task asks for function design plus focused tests and should avoid dependencies unless justified.
- Fallback: If language/schema is unspecified, propose assumptions and request confirmation.
- Warnings: No files were edited and no tests were run.
- Evidence boundary: Static card only; no target language or schema supplied.
- Confidence or answerability note: Moderate confidence on generic design; implementation details need schema/language.
- Next action: Ask for language, fields, optional-field semantics, and desired error behavior.

### Final user-facing answer

Define a pure function that accepts a CSV row string and a schema of required and optional fields, returns a structured record, and represents missing optional values as None or an agreed default. Focus tests on full rows, missing optional trailing fields, empty optional fields, missing required fields, extra columns, quoted commas, and whitespace normalization.

