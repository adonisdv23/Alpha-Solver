# Test Summary

Focused fake-transport tests were added or preserved for:

- direct mode with empty pass-one considerations;
- deterministic clarification for `Make it faster.`;
- bounded `answer_with_assumptions` with ordinary optimization/profiling flags;
- high-risk safety-monitoring and reviewer-concealment blocking;
- pass-one forbidden boundary-claim non-exposure;
- existing pass-two forbidden boundary claims;
- unparseable confidence;
- prompt echo and system echo;
- `/v1/solve` and dashboard non-exposure;
- `answer` and `final_answer` compatibility.

All tests use fake transport only and perform no network calls.
