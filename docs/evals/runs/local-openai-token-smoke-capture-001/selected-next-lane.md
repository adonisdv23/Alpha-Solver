# Selected next lane

Selected next lane: `LOCAL-OPENAI-TOKEN-SMOKE-CAPTURE-RETRY-001`.

Exactly one next lane is selected. This retry lane should run only after the required operator pre-smoke attestation packet is merged and all provider-call permission gates are satisfied.
