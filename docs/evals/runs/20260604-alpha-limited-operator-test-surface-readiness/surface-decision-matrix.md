# Surface Decision Matrix

Lane ID: `ALPHA-LIMITED-OPERATOR-TEST-SURFACE-READINESS-001`

| Surface | Classification | What it can support now | What it cannot support now | Decision rationale |
|---|---|---|---|---|
| `/dashboard/expert-preview` with `MODEL_PROVIDER=local` | Valid only for smoke; blocked for behavior testing if echoing prompts | UI/local smoke, prompt preservation, two-pane rendering, no-provider execution | Limited Alpha answer-behavior operator testing on that surface; portable-contract proof; product-level behavior evidence | The local-smoke spec is explicitly no-network/no-OpenAI and does not claim answer quality. The preview route calls `service.app.solve`; in local mode the service strips `context.route` and calls deterministic `_tree_of_thought` from `alpha_solver_entry`, not `alpha_solver_portable.py`. |
| ChatGPT project-thread / manual prompt-contract simulation | Valid now for portable-contract manual simulation if correctly labeled | The current PR #273 packet's manual portable-contract purpose; prompt-contract/manual simulation observations | Product/runtime evidence, `/v1/solve` evidence, provider evidence, UI evidence, routing evidence, billing/accounting evidence, benchmark validation, MVP validation, production readiness | A manual ChatGPT thread can exercise the wording of the portable prompt contract and aligns with the existing packet when labeled as simulation/manual portable-contract evidence only. |
| `/v1/solve` in local mode | Not evidence for this manual packet; separate future track if requested | General local service execution if separately measured in an authorized lane | Current portable-contract manual packet evidence; product/runtime operator-test evidence in this lane | The endpoint currently uses `alpha_solver_entry`/`alpha-solver-v91-python.py` for local `_tree_of_thought`, not `alpha_solver_portable.py`; this lane also forbids `/v1/solve` use or measurement. |
| `/v1/solve` in provider mode | Requires separate authorization | Potential provider-backed runtime behavior after explicit authorization and readiness review | Current packet evidence, live-provider evidence, exact billing, provider orchestration, broad runtime readiness | Provider calls, provider enablement, and `/v1/solve` measurement are forbidden in this lane. |
| `/dashboard/expert-preview` with live provider access | Requires separate authorization | Potential product UI preview behavior after an approved live-provider lane | Current packet evidence; unapproved provider results; broad readiness or superiority claims | The preview has a live-preview guard and requires explicit enablement/caps. This lane does not authorize enabling live access or importing live outputs. |
| Prior local preview screenshots/observations | Not evidence for this lane | Blocked-surface diagnostic context only | Alpha pass/fail results, scored artifacts, imported operator results | The screenshots were generated before surface readiness was established and must not be treated as operator-test results. |

## Overall surface decision

The current operator packet remains a portable-contract/manual simulation packet. ChatGPT/manual prompt-contract testing is valid for that packet if correctly labeled as simulation/manual portable-contract evidence only. It is not product/runtime evidence. The local supervised preview is smoke-only and blocked for behavior testing if it echoes prompts. Product/runtime operator evidence remains a separate future track requiring `ALPHA-LIMITED-OPERATOR-TEST-SURFACE-FIX-001` or another separately approved surface lane.
