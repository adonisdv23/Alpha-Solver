# Operator review artifacts

Required review artifacts:

- approval record;
- stop-state record when applicable;
- changed-file scope proof;
- command transcript;
- raw artifact inventory;
- reviewer notes.

Artifacts must be local-only and must not promote evidence or contain secrets, credentials, provider outputs, external API responses, browser data, deployment output, or billing data.
