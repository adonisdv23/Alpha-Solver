# Confirmation copy

Future operator confirmation copy should state:

> I confirm this run is limited to the selected local Self Operator lane and allowed changed-file scope. I understand provider calls, external APIs, credentials, browser automation, deployment, billing, `/v1/solve` exposure, dashboard exposure, fallback, source-artifact mutation, and evidence promotion are forbidden.

The exact future copy may be adjusted by a controlling packet, but it must preserve every forbidden surface.
