# Non-actions

Docs-only approval and stop-state spec. This does not implement controls, execute actions, call providers, call external APIs, modify runtime, deploy, merge, bill, expose routes, control browsers, or promote evidence.
