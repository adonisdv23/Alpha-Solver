# Operator Evidence Summary

The operator provided screenshot evidence of the local console loaded at `http://127.0.0.1:8765`. Only the observations below are recorded.

- local console opens in browser at `http://127.0.0.1:8765`
- header says `Alpha Solver local smoke test console`
- `LOCAL ONLY` badge is visible
- evidence boundary panel is visible
- route preview panel is visible
- route preview task textarea is visible
- `Preview route only` button is visible
- route preview initially says `No route preview yet`
- route preview evidence boundary is visible
- provider/local execution authorized is shown as `false`
- tool execution authorized is shown as `false`
- bounded smoke check section is visible
- mode dropdown is visible
- model dropdown is visible
- prompt box is visible
- character counter is visible
- `Run bounded smoke check` button is visible
- result summary is visible and says `No run yet`
- sanitized JSON panel is visible
- `Copy sanitized JSON` button is visible
- setup checklist is visible
- no API key field is visible in the UI
