# Non-actions

This lane did not score, fill scores, choose winners, compute totals, write rationales, interpret results, unblind, commit an A/B key, change source outputs, call providers, run hosted models, run local models, execute tools, browse, mutate Google Sheets, add dependencies, expose `/v1/solve`, expose dashboard/public API behavior, deploy, or perform source-map work.
