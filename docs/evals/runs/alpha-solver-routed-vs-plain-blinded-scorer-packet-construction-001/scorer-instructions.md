# Scorer instructions

Use only `scorer-packet.md`, the per-task files in `blinded-tasks/`, `rubric.md`, and `scoring-sheet-template.md` during blind scoring.

Do not inspect source output directories, route metadata, Git history for identity reconstruction, source maps, provider dashboards, local model outputs, runtime endpoints, `/v1/solve`, Google Sheets, external/current research, or any A/B key.

Do not score unless a future lane separately authorizes scoring. In this construction lane, leave all score, preference, rationale, winner, aggregate, and interpretation fields blank.
