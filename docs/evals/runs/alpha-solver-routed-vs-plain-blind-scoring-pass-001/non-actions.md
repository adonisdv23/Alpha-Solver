# Non-actions

This lane did not unblind, inspect source identities, inspect source outputs, inspect route metadata, inspect or commit an A/B key, inspect or commit a source map, change source outputs, change the scorer packet, run Alpha runtime, invoke `/v1/solve`, call providers, run hosted or local models, execute tools for task answering, browse, use current external research, mutate Google Sheets, add dependencies, expose dashboards or public APIs, or deploy.
