# Blocked Work

This lane does not authorize:

- production `/v1/solve` exposure;
- dashboard exposure;
- hosted provider fallback;
- hosted provider calls;
- local model calls during tests;
- real network calls during tests;
- evidence-model promotion;
- MVP validation claims;
- production readiness claims;
- benchmark claims;
- Alpha superiority claims;
- broad runtime readiness claims;
- billing changes;
- Google Sheets updates;
- smoke execution;
- result import;
- changes to Batch C artifacts;
- reopening the local LLM runtime closeout.
