# Confirmation record schema

Proposed local-only fields:

- `operator_confirmation_required`
- `operator_confirmation_present`
- `confirmation_text`
- `confirmed_scope`
- `confirmed_at_utc`
- `stop_if_missing`

The record must not store credentials, secrets, payment data, provider output, browser data, external API responses, or promotion labels.
