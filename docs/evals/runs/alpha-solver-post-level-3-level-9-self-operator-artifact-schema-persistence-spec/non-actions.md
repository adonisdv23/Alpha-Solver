# Non-actions

Docs-only artifact specification. This does not create artifacts, implement persistence code, run models, call providers, call external APIs, access credentials, control browsers, deploy, bill, or promote evidence.
