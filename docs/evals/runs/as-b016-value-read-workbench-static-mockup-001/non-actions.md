# Non-actions

This lane does not implement runtime code, modify provider execution, modify Operator Console runtime behavior, modify routes, add live UI behavior, add routes, add POST routes, create a real-run cockpit, call providers, run hosted/local models, expose or invoke `/v1/solve`, score outputs, unblind results, reveal source identities, perform final interpretation, mutate Google Sheets or external ledgers, or create a generic LLM playground.
