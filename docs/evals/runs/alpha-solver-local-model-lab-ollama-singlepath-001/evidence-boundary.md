# Evidence boundary

This packet is local-only lab scaffolding. It does not run Ollama by itself, call hosted providers, use tokens, install models, pull models, sweep a model registry, use private data, expose `/v1/solve`, expose dashboard routes, expose public APIs, add routing, add council behavior, score outputs, run benchmarks, or promote evidence.

Any real local run must remain on `http://127.0.0.1:11434/api/chat` with exact model `gemma3:4b` and synthetic fixtures only.
