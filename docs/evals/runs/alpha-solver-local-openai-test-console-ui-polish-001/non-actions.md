# Non-actions

This lane did not run OpenAI, Ollama, or local models.

This lane did not expose `/v1/solve`, deploy, mutate Google Sheets, generate eval outputs, score outputs, inspect raw prior eval outputs, unblind, perform source-map work, add dependencies, add external frontend scripts, add external telemetry, add result persistence, or add an API key input field.
