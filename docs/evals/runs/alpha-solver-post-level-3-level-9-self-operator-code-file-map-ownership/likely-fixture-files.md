# Likely fixture files

These paths are likely inert fixture candidates for a future first-code lane. They are identified only as proposed paths and are not created by this packet.

## Future first-code lane may modify

- `tests/fixtures/self_operator/README.md`
- `tests/fixtures/self_operator/inert_operator_request.json`
- `tests/fixtures/self_operator/inert_approval_record.json`
- `tests/fixtures/self_operator/inert_artifact_manifest.json`
- `tests/fixtures/self_operator/inert_preflight_result.json`

## Fixture boundary

Fixtures must contain synthetic, inert data only. They must not contain secrets, credentials, provider outputs, external API responses, billing data, browser data, deployment output, source-artifact payloads, or evidence-promotion labels.
