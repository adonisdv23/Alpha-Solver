# DEF-002 gap-closure plan

This table converts every finding from the DEF-002 review packet into a closure
planning row. It is a plan only: no runtime code, configuration, credentials,
auth, tenancy, dashboard, provider, model, or API behavior is changed here.

Severity and disposition are inherited from the DEF-002 `risk-register.md` unless
this plan adds sequencing context.

| Finding | Disposition | Severity | Blast radius | Prerequisites | Likely files for future lane | Test needs for future lane | Evidence needed to close |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RR-01 CORS wildcard origin with credentials | Must-fix | Medium | Browser/API exposure; any deployment relying on default `CORSMiddleware` settings | Operator decision on allowed origins and dev-vs-deployed defaults | `alpha/core/config.py`, `service/app.py`, `.env.example`, docs describing CORS configuration | Unit/config tests for default origins; API/app construction test proving wildcard+credentials is not the default; docs/static check for env examples | Code diff showing tightened defaults; tests proving safe defaults and explicit opt-in for permissive local development; updated operator docs |
| RR-02 plaintext provider secrets at rest in `FileSecretsBackend` | Must-fix | High | Provider API keys stored through dashboard settings; local operator filesystem; audit/display controls remain separate | Decide storage backend strategy: OS keyring, envelope encryption, or fail-closed no-persistence mode; migration behavior for existing plaintext files; file-mode policy | `alpha/webapp/routes/settings.py`, dashboard settings tests, docs/API key handling docs, possibly `.env.example` for backend selection | Unit tests for encrypted/protected write path; restrictive directory/file permissions; masked display remains masked; migration/legacy plaintext handling; no secret in logs/audit | Before/after tests; code evidence of protected at-rest storage or explicit no-persistence design; file mode assertions; migration/non-leak evidence; docs describing operational key management |
| RR-03 insecure default API key and dashboard password | Must-fix | High | API auth, dashboard mount gate, local/dev deployments, any operator that fails to override defaults | Decide developer ergonomics: fail-fast when missing secrets, explicit dev profile, generated local-only secret, or test-only fixture defaults | `alpha/core/config.py`, `alpha/webapp/routes/auth.py`, `service/security.py`, `service/app.py`, `.env.example`, auth/dashboard tests | Tests that production/default config has no known usable secret; dashboard cannot mount with default password; API auth rejects known default unless explicit test/dev profile; docs updated | Code/config evidence removing or quarantining known defaults; tests proving fail-closed behavior; docs with secure setup instructions and no readiness claim |
| RR-04 provider prompt data sharing and missing end-user disclosure | Accepted residual candidate / governance lane | Medium | Provider-enabled inference only; user prompts sent to third party when explicitly enabled | Operator-approved data-sharing and retention wording; provider identity and policy references; user-facing disclosure placement | `docs/`, provider/operator runbooks, possibly dashboard/provider setup copy in a later UX lane | Documentation checks; no provider calls; optional UI copy tests only if UI text changes in a later lane | Operator acceptance packet plus committed disclosure of what is sent, to whom, retention boundary, opt-in gate, and non-readiness limitations |
| RR-05 divergent data classification registries | Must-fix | Medium | Policy interpretation, evidence classification, future compliance checks | Decide authoritative registry and migration/alias strategy; preserve legacy file if required by specs | `config/data_classification.yaml`, `registries/data_classification.yaml`, docs referencing classification policy, static checkers if any | Static test/check that exactly one authoritative policy or documented precedence exists; schema validation for registry; docs path checks | Diff establishing authoritative policy/precedence; test evidence preventing divergence; updated docs referencing the source of truth |
| RR-06 duplicated dependency declarations drift | Must-fix | Low | Developer installs, CI, dependency-review accuracy | Choose one source of truth or documented sync process; coordinate with lockfile lane | `requirements.txt`, `requirements-dev.txt`, `pyproject.toml`, dependency docs, CI docs | Static check comparing declared runtime deps; install/smoke docs check if practical | Evidence that dependency declarations are synchronized or one is generated from the other; check preventing drift |
| RR-07 no lockfile/hash pinning | Must-fix | Medium | Fresh installs, CI, supply-chain reproducibility | Choose lock mechanism (`pip-tools`, uv, Poetry, or hash-pinned requirements); decide update cadence | New lock/constraints file, `requirements*.txt`, `pyproject.toml`, docs/CI if in scope | Reproducible install check where practical; hash/lock validation; dependency audit command if network/tooling allows | Committed lock/hash artifact; documented update process; validation output showing resolver uses reviewed pins/hashes |
| RR-08 vendored/shimmed third-party libs without provenance | Must-fix | Medium | Root-level vendored modules; dependency scanning blind spots; patch provenance | Inventory exact vendored paths, versions, upstream sources, local modifications; decide replace vs retain | `slowapi/`, `prometheus_client/`, `prometheus_fastapi_instrumentator/`, `jsonschema/`, `jsonlines_compat.py`, provenance docs | Static inventory check; import behavior tests if replacements occur; provenance doc validation | Vendored dependency inventory with source/version/patch status; either replacement with declared deps or accepted provenance controls; tests proving behavior preserved if changed |
| RR-09 `/v1/solve` auth/tenancy model unconfirmed | Must-fix | Medium | `/v1/solve` exposure path, auth boundary, tenant isolation, rate limiting/accountability | Product/security decision: API-key-only is intentional and documented, or JWT/tenant middleware must be wired before exposure | `service/app.py`, `service/middleware/*.py`, `service/security.py`, `service/tenancy/*`, auth/tenant docs and tests | Route tests proving selected auth model; tenant isolation/rate limit tests if mounted; negative auth tests; no public exposure in test setup | Committed decision or implementation proving intended auth/tenancy model; tests for rejected unauthenticated/incorrect-tenant access; docs stating exposure remains not ready until gates pass |
| RR-A1 pattern-based redaction coverage limits | Accepted residual candidate / hardening optional | Low | Self Operator artifacts and any operator-supplied text passed through regex/keyword redaction | Operator review of deployment secret formats; optional expansion of patterns if needed | `alpha/self_operator/redaction.py`, redaction tests, operator docs | Unit tests for representative deployment secret formats; docs acknowledgement if accepted | Operator acceptance of limits or expanded test-backed redaction coverage; no claim of perfect redaction |
| Operational residual: JWT keystore management | Accepted residual candidate / deployment responsibility | Medium | Deployment key rotation and public-key YAML custody | Operator deployment key-management process | Deployment docs, `service/auth/jwt_utils.py` tests only if behavior changes | Docs/static checks; no private key material in repo | Operator acceptance or deployment runbook specifying rotation/custody; no secrets committed |
| Operational residual: evidence payload hygiene | Accepted residual candidate / deployment responsibility | Medium | Evidence API callers and stored artifacts | Operator policy for evidence payload review and secret exclusion | Evidence API docs/runbooks, possibly evidence API validators in a future lane | Documentation checks; optional negative tests if validators are added later | Operator policy or code guardrails showing callers must not submit secrets; redaction/validation evidence if implemented |

## Must-fix before DEF-002 closeout

RR-01, RR-02, RR-03, RR-05, RR-06, RR-07, RR-08, and RR-09 must be remediated or
explicitly deferred by an operator decision before DEF-002 can move toward
operator risk acceptance and closeout.
